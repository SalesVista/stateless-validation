{
  "name": "@salesvista/stateless-validation",
  "version": "3.0.0",
  "description": "Reusable validation logic as pure functions",
  "main": "lib/index.js",
  "files": [
    "lib/index.js"
  ],
  "scripts": {
    "prepare": "babel src -d lib",
    "pretest": "standard && npm run prepare",
    "test": "tap --cov test.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "release": "standard-version"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SalesVista/stateless-validation.git"
  },
  "contributors": [
    "nexdrew",
    "broanius"
  ],
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SalesVista/stateless-validation/issues"
  },
  "homepage": "https://github.com/SalesVista/stateless-validation#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "coveralls": "^3.0.2",
    "standard": "^12.0.1",
    "standard-version": "^4.4.0",
    "tap": "^12.1.1"
  },
  "dependencies": {
    "email-addresses": "^3.0.3"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
